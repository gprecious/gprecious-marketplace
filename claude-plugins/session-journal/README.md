# session-journal

Claude Code plugin wrapper for the shared `session-journal` Obsidian logging core.

See `docs/session-journal.md` in the marketplace root for install, vault layout, hook behavior, wiki draft curation, Slack reporting, and verification details.
